# Copy to .env and fill in. .env is gitignored.
# Key: https://console.groq.com/keys
GROQ_API_KEY=

# Verify against https://console.groq.com/docs/models before trusting this —
# Groq retires ids, and a retired one fails with a 404 that reads like auth.
GROQ_MODEL=llama-3.3-70b-versatile
